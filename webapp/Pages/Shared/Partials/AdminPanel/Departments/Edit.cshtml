@model webapp.Pages.AdminModel

<h2>Edytuj wydział</h2>

<form method="post" asp-page-handler="EditDepartment">
    <input type="hidden" name="id" value="@Model.SelectedId" />
    <div class="mb-3">
        <label for="name" class="form-label">Nazwa</label>
        <input type="text" class="form-control" id="name" name="name" value="@Model.Departments.FirstOrDefault(d => d.Id == Model.SelectedId)?.Name" required />
    </div>
    <button type="submit" class="btn btn-primary">Zapisz</button>
</form>